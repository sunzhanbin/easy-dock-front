import { memo, FC } from "react";
import styles from "./index.module.scss";

const NodeFlowConfig: FC = () => {
  return <div className={styles.container}></div>;
};

export default memo(NodeFlowConfig);
