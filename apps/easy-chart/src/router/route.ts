const staticRoutes = {
  INDEX: '/report-detail',
  REPORT_DETAIL: '/report-detail'
};

export default staticRoutes;

export const dynamicRoutes = {
  // toReportDetail(appId: number) {
  //   return staticRoutes.REPORT_DETAIL.replace(/:appId/, String(appId));
  // },
};
