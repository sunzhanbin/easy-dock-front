export type Member = {
  name: string;
  id: number | string;
  avatar?: string;
};

export type Dept = {
  name: string;
  id: number | string;
  avatar?: string;
};

export type Role = {
  id: number | string;
  name: string;
};
